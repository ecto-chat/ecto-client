import { create } from 'zustand';

// eslint-disable-next-line @typescript-eslint/no-empty-object-type
interface DmState {
  // TODO: Add state shape
}

export const useDmStore = create<DmState>()((_set) => ({
  // TODO: Initial state and actions
}));
